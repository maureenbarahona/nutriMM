---
description: A workflow to start the local Next.js development/preview server.
---

Follow these steps to run a development preview of the project:

### Start Development Server
Start the development server on port 9002:
```bash
npm run dev
```

The preview will be available locally at `http://localhost:9002`.
